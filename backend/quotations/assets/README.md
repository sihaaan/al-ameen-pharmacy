`al-ameen-website-logo.png` is an unchanged copy of
`frontend/public/al-ameen-pharmacy-logo-transparent.png`.

Tax invoice PDFs use this local website artwork on a white background. Packaging
it with the backend keeps the logo available when Railway deploys only the
backend directory. Do not replace it with the legacy invoice's pharmacy logo.
